package leetcode

import (
	"fmt"
	"testing"
)

func Test_isPalindromeStr(t *testing.T) {
	s := "aa"
	// for i := 0; i < len(s); i++ {
	// 	fmt.Printf("%d\t", s[i])
	// }
	fmt.Println(isPalindromeStr(s))
}
